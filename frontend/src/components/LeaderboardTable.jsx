/**
 * LeaderboardTable — renders rank, name, points, and rankTrend arrow.
 *
 * rankTrend display (SRS §10):
 *   positive → up-arrow (improved)
 *   negative → down-arrow (dropped)
 *   null     → "New" label (no prior snapshot)
 */

// TODO: implement
export default function LeaderboardTable({ entries }) {
  return null;
}
