/**
 * SportBreakdownChart — pie or bar chart of points by sport.
 *
 * Data shape: { running: number, gym: number, ... }
 * Uses recharts (SRS §10).
 */

// TODO: implement
export default function SportBreakdownChart({ breakdown }) {
  return null;
}
