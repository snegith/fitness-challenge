/**
 * ActivityHistoryList — renders the user's activity log.
 *
 * Data shape: [{ activityId, sportType, points, recordedAt }, ...]
 * Empty state: display a friendly "No activities yet" message.
 */

// TODO: implement
export default function ActivityHistoryList({ activities }) {
  return null;
}
