/**
 * VolumeOverTimeChart — line chart of points per IST calendar day.
 *
 * Data shape: [{ date: "YYYY-MM-DD", points: number }, ...]
 * Uses recharts LineChart (SRS §10).
 */

// TODO: implement
export default function VolumeOverTimeChart({ data }) {
  return null;
}
