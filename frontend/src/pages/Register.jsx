/**
 * Register page — POST /api/auth/register
 *
 * On success: store token, navigate to Dashboard.
 * On 409: display "Name already registered" message.
 * On 400: display validation error.
 */

// TODO: implement
export default function Register() {
  return null;
}
