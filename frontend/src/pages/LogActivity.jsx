/**
 * Log Activity page — POST /api/activities
 *
 * Renders a sport picker; conditionally renders the correct metric field:
 *   distance sports → distanceKm input
 *   duration sports → durationSec input
 *   daily_steps     → stepCount input
 *
 * On success: display inline confirmation with awarded points.
 * Client-side validation blocks submission of empty or invalid fields
 * before the API call (SRS §11.7).
 */

// TODO: implement
export default function LogActivity() {
  return null;
}
