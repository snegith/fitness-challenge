/**
 * Dashboard page — GET /api/users/{id}/dashboard (requires Bearer token)
 *
 * Renders:
 *   - ActivityHistoryList
 *   - VolumeOverTimeChart
 *   - SportBreakdownChart
 *   - "Switch user" action → clears token, navigate to Register/Login
 *
 * Empty-activity case renders zeros and empty charts, not an error state.
 */

// TODO: implement
export default function Dashboard() {
  return null;
}
